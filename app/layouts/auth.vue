<template>
  <main class="min-h-dvh flex items-center justify-center bg-stone-50 px-[max(env(safe-area-inset-left),1.5rem)] pt-[max(env(safe-area-inset-top),1.5rem)] pb-[max(env(safe-area-inset-bottom),1.5rem)]">
    <slot />
  </main>
</template>
